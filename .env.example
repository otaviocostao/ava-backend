# =========================================
# CONFIGURAÇÃO DO BANCO DE DADOS - AVA BACKEND
# =========================================
#
# ESCOLHA UMA DAS OPÇÕES ABAIXO:
#

# ┌─────────────────────────────────────────┐
# │ OPÇÃO A: PostgreSQL Local (via Docker)  │
# └─────────────────────────────────────────┘
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=sua_senha_aqui
DB_DATABASE=ava_db

JWT_SECRET=12345678
JWT_EXPIRES_IN=1d


POSTGRES_USER=postgres
POSTGRES_PASSWORD=sua_senha_aqui
POSTGRES_DB=ava_db
POSTGRES_PORT=5432

# ┌─────────────────────────────────────────┐
# │ OPÇÃO B: Supabase (Recomendada)         │
# └─────────────────────────────────────────┘
# Cole a string de conexão do Supabase aqui (Session Mode)
# Formato: postgresql://postgres.[ID_PROJETO]:[SENHA]@aws-0-regiao.pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres.[SEU_ID_PROJETO]:[SUA_SENHA]@aws-0-regiao.pooler.supabase.com:6543/postgres

SUPABASE_URL=https://[SEU_ID_PROJETO].supabase.co
SUPABASE_SERVICE_ROLE_KEY=SUPABASE_SERVICE_ROLE_KEY


JWT_SECRET=12345678
JWT_EXPIRES_IN=1d


# =========================================
# COMO USAR:
# =========================================
#
# 1. Para PostgreSQL Local:
#    - Configure DB_USERNAME, DB_PASSWORD, POSTGRES_USER, POSTGRES_PASSWORD
#    - Execute: docker-compose up -d
#    - REMOVA ou COMENTE a linha DATABASE_URL
#
# 2. Para Supabase:
#    - Configure apenas DATABASE_URL com a string do Supabase
#    - REMOVA ou COMENTE as variáveis DB_* e POSTGRES_*
#
# 3. Copie este arquivo:
#    cp env-config-example.txt .env
#
# 4. Edite o .env com suas credenciais reais
#
# =========================================
# DETECÇÃO AUTOMÁTICA:
# =========================================
#
# O sistema detecta automaticamente:
# ✅ Se DATABASE_URL estiver definida → Usa Supabase
# ✅ Se DATABASE_URL não estiver definida → Usa PostgreSQL local
#
# Você pode alternar entre as opções editando apenas este arquivo!
# =========================================
