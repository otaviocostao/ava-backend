version: '3.8'

services:
  app_backend:
    container_name: ava-backend-production
    build: .
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production